name: " Upload Guessing_Game"
language: "Python"
build: "On Pycharm"
Release:
    type: [ public ]
on:
  push:
    branches: [ master ]
  pull_request: [ master ]

Jobs:
  build:
     steps:
    - name: 'Enjoy the game'
      run: guess the number
    - name: 'Find bug'
      run : Go to pull request
