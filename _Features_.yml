name: " Upload Guessing_Game"
script: "Python"
build: "On Pycharm"
Release:
    type: [ public ]
on:
  push:
    branches: [ master ]
  pull_request:
   branches: [ master ]
   
steps:
    - name: 'Enjoy the game'
      run: guess the number
    - name: 'Find error'
      run : Go to pull request
